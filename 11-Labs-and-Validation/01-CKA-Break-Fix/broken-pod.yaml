apiVersion: v1
kind: Pod
metadata:
  name: "nginx-frontend"
  labels:
    tier: frontend
    env: production
spec:
  containers:
    - name: nginx
      image: nginx-1.21.3
      ports:
        - containerPort: 80
  nodeSelector:
    disk_type: ssd
  restart_policy: Always
